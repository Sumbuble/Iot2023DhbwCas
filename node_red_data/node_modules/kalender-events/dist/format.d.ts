import { iCalEvent, IKalenderEvent, Config } from "./interfaces";
export declare function formatDate(event: iCalEvent | IKalenderEvent | undefined, _date: Date, _end: Date, withTime: boolean, config: Config): string;
